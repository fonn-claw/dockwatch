---
phase: 04-dashboard-seed-data-polish
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/app/globals.css
  - src/components/layout/app-shell.tsx
  - src/components/layout/sidebar.tsx
  - src/app/(app)/assets/page.tsx
  - src/app/(app)/schedules/page.tsx
  - src/app/(app)/compliance/page.tsx
  - src/app/(app)/costs/page.tsx
  - src/app/(app)/work-orders/page.tsx
autonomous: true
requirements:
  - DEMO-08
  - DEMO-09

must_haves:
  truths:
    - "All data tables scroll horizontally on mobile (no content overflow)"
    - "Dashboard status cards stack to single column on mobile"
    - "Touch targets are at minimum 44px on interactive elements"
    - "Consistent 24px page padding, 16px card gaps throughout"
    - "Industrial color palette with safety status colors used consistently"
    - "Loading skeleton states exist for dashboard"
    - "Empty state messaging displays when no data exists"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Consistent spacing and responsive utilities"
  key_links:
    - from: "src/app/globals.css"
      to: "all page components"
      via: "Tailwind utility classes"
      pattern: "responsive|mobile"
---

<objective>
Polish the entire app for LinkedIn showcase quality: mobile responsiveness, consistent spacing, professional typography, and loading/empty states.

Purpose: DockWatch will be showcased on LinkedIn. Every page must look professional at both desktop and 375px mobile width. Consistent spacing, touch targets, and industrial design language must carry across all views.

Output: All pages polished with responsive design, consistent spacing, and professional styling.
</objective>

<execution_context>
@/home/fonn/.claude/get-shit-done/workflows/execute-plan.md
@/home/fonn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-dashboard-seed-data-polish/04-CONTEXT.md
@.planning/phases/04-dashboard-seed-data-polish/04-RESEARCH.md
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mobile responsiveness pass across all pages</name>
  <files>
    src/app/globals.css
    src/app/(app)/assets/page.tsx
    src/app/(app)/schedules/page.tsx
    src/app/(app)/compliance/page.tsx
    src/app/(app)/costs/page.tsx
    src/app/(app)/work-orders/page.tsx
    src/components/layout/app-shell.tsx
    src/components/layout/sidebar.tsx
  </files>
  <read_first>
    src/app/globals.css
    src/app/(app)/assets/page.tsx
    src/app/(app)/schedules/page.tsx
    src/app/(app)/compliance/page.tsx
    src/app/(app)/costs/page.tsx
    src/app/(app)/work-orders/page.tsx
    src/components/layout/app-shell.tsx
    src/components/layout/sidebar.tsx
  </read_first>
  <action>
Perform a mobile responsiveness pass across all existing pages. Test target: 375px width (iPhone SE).

**globals.css updates:**
- Add utility class `.table-responsive` that wraps tables in `overflow-x-auto -mx-4 px-4` on mobile for horizontal scroll.
- Ensure base spacing variables are consistent: page padding uses `p-4 md:p-6` pattern.
- Add `.touch-target` utility: `min-h-[44px] min-w-[44px]` for mobile tap targets.

**Layout (app-shell.tsx, sidebar.tsx):**
- Verify sidebar hamburger menu works on mobile. If sidebar uses fixed width, ensure it overlays on mobile (not pushes content).
- On mobile, main content area should use full width with no sidebar visible by default.
- Ensure header height and padding are consistent.

**Assets page:**
- Wrap data table in `.table-responsive` div or `overflow-x-auto`.
- Filter controls: on mobile, collapse into a summary row with a "Filters" button that toggles expansion.
- Asset detail sheet/panel: ensure it renders full-width on mobile.

**Schedules page:**
- Wrap schedule table in overflow-x-auto.
- Filter panel collapses on mobile.
- New Schedule dialog: ensure form fields don't overflow on mobile.

**Compliance page:**
- Status cards: stack to single column on mobile (grid-cols-1).
- Audit trail table: overflow-x-auto wrapper.
- Safety-critical section: cards stack vertically.

**Costs page:**
- Summary cards: grid-cols-1 on mobile, grid-cols-2 on md+, grid-cols-4 on lg+.
- Budget comparison bars: ensure they don't overflow.
- Breakdown tables: overflow-x-auto.

**Work orders page:**
- Work order cards: already stack vertically. Verify card content doesn't overflow.
- Filter controls: collapse on mobile.
- Ensure status transition buttons have 44px touch targets.

For each page, the pattern is:
1. Read the file
2. Add responsive Tailwind classes where grids break on mobile
3. Wrap tables in overflow-x-auto
4. Ensure buttons/interactive elements have min-h-[44px] on mobile
5. Test that no content overflows at 375px conceptual width
  </action>
  <verify>
    <automated>cd /home/fonn/.openclaw/workspace/projects/dockwatch && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All table containers have overflow-x-auto class
    - Grid layouts use grid-cols-1 as base with responsive breakpoints for larger screens
    - Interactive buttons have min-h-[44px] or equivalent touch-friendly sizing
    - Sidebar works as overlay on mobile, not pushing content
    - Filter panels have mobile-collapsed state or are scrollable
    - npm run build passes
  </acceptance_criteria>
  <done>All pages are mobile-responsive at 375px width: tables scroll horizontally, grids stack vertically, touch targets meet 44px minimum, filter panels collapse, sidebar overlays.</done>
</task>

<task type="auto">
  <name>Task 2: UI polish pass for consistent spacing, typography, and states</name>
  <files>
    src/app/globals.css
    src/app/(app)/assets/page.tsx
    src/app/(app)/schedules/page.tsx
    src/app/(app)/compliance/page.tsx
    src/app/(app)/costs/page.tsx
    src/app/(app)/work-orders/page.tsx
  </files>
  <read_first>
    src/app/globals.css
    src/app/(app)/assets/page.tsx
    src/app/(app)/schedules/page.tsx
    src/app/(app)/compliance/page.tsx
    src/app/(app)/costs/page.tsx
    src/app/(app)/work-orders/page.tsx
  </read_first>
  <action>
Polish every page for LinkedIn-showcase quality.

**Consistent spacing (per CONTEXT.md locked decisions):**
- Page padding: `p-6` (24px) on all page containers. If any use `p-4` or `p-8`, normalize to `p-4 md:p-6`.
- Card gaps: `gap-4` (16px) on all grid containers.
- Inner card padding: `p-4` (16px) standard, headers get appropriate CardHeader spacing.

**Typography hierarchy:**
- Page titles: `text-2xl font-bold tracking-tight` (24px bold).
- Section headers: `text-lg font-semibold` (18px semibold).
- Body text: `text-sm` (14px) for table cells and descriptions.
- Muted text: `text-muted-foreground` for secondary information.
- Ensure every page has a consistent title + subtitle pattern matching dashboard.

**Card styling consistency:**
- All cards use shadcn Card with consistent `shadow-sm` (subtle shadows, not heavy).
- Consistent `rounded-lg` border radius.
- All status indicator colors use the same palette: red-50/red-600 (overdue), yellow-50/yellow-600 (due soon), green-50/green-600 (on track).
- Verify all badge components use consistent colors.

**Empty states:**
- Add empty state messaging to pages that could have no data:
  - Assets: "No assets found. Create your first asset to get started."
  - Work Orders: "No work orders match your filters." (with filter context)
  - Schedules: "No maintenance schedules yet. Create a schedule to begin tracking."
  - Use a centered layout with muted icon + text for empty states.

**Hover/interaction states:**
- Table rows: `hover:bg-muted/50` for subtle hover feedback.
- Buttons: ensure all have visible hover and focus states (shadcn defaults cover this).
- Cards with click actions: `hover:shadow-md transition-shadow` for visual feedback.

**Industrial color palette verification:**
- Verify globals.css has the industrial color scheme: slate-700 tones for dark elements, white/slate-50 backgrounds.
- Status colors: consistent red/yellow/green usage for overdue/due-soon/on-track across ALL pages (dashboard, compliance, schedules).
- Ensure no conflicting color usage (e.g., red for non-error purposes).
  </action>
  <verify>
    <automated>cd /home/fonn/.openclaw/workspace/projects/dockwatch && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All pages use text-2xl font-bold tracking-tight for page titles
    - All grid containers use gap-4 for card spacing
    - All Card components have shadow-sm
    - Empty state messages exist in assets, work-orders, and schedules pages
    - Table rows have hover:bg-muted/50 or equivalent hover state
    - Status colors are consistent: red for overdue, yellow for due-soon, green for on-track
    - npm run build passes
  </acceptance_criteria>
  <done>All pages have consistent 24px page padding, 16px card gaps, uniform typography hierarchy, shadow-sm cards, empty state messaging, hover states, and consistent industrial color palette with safety status colors.</done>
</task>

</tasks>

<verification>
- `npm run build` passes without errors
- All pages use consistent spacing and typography
- No content overflow at 375px conceptual width
- Status colors consistent across dashboard, compliance, and schedule pages
</verification>

<success_criteria>
- Every page is mobile-responsive at 375px (tables scroll, grids stack, touch targets 44px+)
- Consistent spacing: 24px page padding, 16px card gaps throughout
- Typography hierarchy: 24px titles, 18px section headers, 14px body
- Empty states with friendly messaging on data-less pages
- Industrial color palette with safety status colors used consistently
- App looks professional enough for LinkedIn showcase
</success_criteria>

<output>
After completion, create `.planning/phases/04-dashboard-seed-data-polish/04-03-SUMMARY.md`
</output>
